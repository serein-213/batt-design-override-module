<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许应用内发起未知来源安装（Android 8+ 自更新需要） -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
            android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <activity android:name="com.override.battcaplsp.ui.MainActivity"
                android:exported="true"/>
            
            <!-- 模块设置入口 - 直接跳转到主界面 -->
            <activity-alias
                android:name="com.override.battcaplsp.ModuleSettings"
                android:exported="true"
                android:enabled="true"
                android:targetActivity="com.override.battcaplsp.ui.MainActivity"
                android:label="@string/module_settings_title">
                <!-- LSPosed 模块设置入口 -->
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
                </intent-filter>
            </activity-alias>
            
            <activity-alias
                android:name="com.override.battcaplsp.Launcher"
                android:exported="true"
                android:enabled="true"
                android:targetActivity="com.override.battcaplsp.ui.MainActivity">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity-alias>
        <receiver android:name="com.override.battcaplsp.BootCompletedReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="com.override.battcaplsp.SettingsProvider"
            android:authorities="com.override.battcaplsp.provider"
            android:exported="true"
            android:grantUriPermissions="true" />
        <!-- FileProvider 用于暴露下载的 APK (content://) 避免 FileUriExposedException -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.override.battcaplsp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Override battery capacity in Settings basic info" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>

</manifest>
